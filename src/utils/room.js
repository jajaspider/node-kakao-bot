function getRoomTypes(roomNumber) {
  //DB에서 type가져오는 로직
  //types 배열 리턴
  // 방번호 | type
  // 1 | LOSTARK_COMMAND
  // 1 | MAPLESTORY_COMMAND
  // 여기서 common_command 추가
}

module.exports = {
  getRoomTypes,
};
